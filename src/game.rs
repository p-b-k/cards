////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Create the data structures for the seahaven game
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

use crate::cards::Card;

#[derive(Clone, Debug)]
pub struct Stack {
    pub cards: Vec<Card>,
}

pub struct Table {
    pub found: [u8; 4],
    pub free: [Card; 4],
    pub build: [Vec<Card>; 10],
}
