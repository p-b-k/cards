////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Seahaven Tableeau TUI
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

use crate::game::Table;

#[derive(Clone, Debug, PartialEq)]
pub enum Mode {
    Free(u8),
    Build(u8),
}

#[derive(Clone, Debug)]
pub struct Tableau {
    pub tab: Table,
    pub mode: Mode,
}
